/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['assets.aceternity.com', "media.istockphoto.com","aceternity.com", "images.unsplash.com"],
      },
};

export default nextConfig;
