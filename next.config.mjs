/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['assets.aceternity.com', "media.istockphoto.com","aceternity.com"],
      },
};

export default nextConfig;
